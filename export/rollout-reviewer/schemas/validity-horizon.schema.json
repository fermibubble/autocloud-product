{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "validity-horizon.schema.json",
  "title": "P6 - Validity horizon fields",
  "description": "Knowledge requires a clock: the verdict itself carries an expiry and a reassessment trigger. valid_through is checkpoint-relative (T+5 | T+15 | T+30 | end-of-ladder) or an ISO timestamp ONLY when derived from an envelope's fresh_until - never an invented time. Referenced as $defs by the epistemic record.",
  "$defs": {
    "valid_through": {
      "oneOf": [
        {
          "type": "string",
          "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}(\\.[0-9]+)?(Z|[+-][0-9]{2}:[0-9]{2})?$"
        },
        { "type": "string", "pattern": "^(T\\+(5|15|30)|end-of-ladder)$" }
      ]
    },
    "reassess_if": {
      "type": "string",
      "minLength": 10
    }
  }
}
